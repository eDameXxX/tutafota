<Page
    x:Class="Fotooo.Views.ContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fotooo.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:model="using:Fotooo.Models">

    <Page.Resources>
        <Style x:Key="NotesListViewStyle" TargetType="ListView">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Top" />
            <Setter Property="Height" Value="Auto" />
            <Setter Property="Width" Value="Auto" />
            <Setter Property="Margin" Value="10,10" />
        </Style>
    </Page.Resources>
    
    <Grid Background="LightCoral">

        <Image Source="/Assets/notes_symbol.png" 
               HorizontalAlignment="Stretch" 
               Height="200" 
               VerticalAlignment="Stretch" 
               Width="200">

        </Image>

        <ListView Name="NotesListView" Style="{StaticResource NotesListViewStyle}" ItemsSource="{x:Bind Notes}"  BorderBrush="red" BorderThickness="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Note">
                    <ListViewItem Width="auto" BorderThickness="2" BorderBrush="yellow" Margin="-8,5" Padding="2" HorizontalContentAlignment="Stretch">
                        <Grid BorderBrush="AliceBlue" BorderThickness="1" Width="auto" Padding="3,5,3,20" Background="Snow">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="{x:Bind Title}" Foreground="blue" />
                            <TextBlock Grid.Row="1" Text="{x:Bind Content}" />
                        </Grid>
                    </ListViewItem>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

    </Grid>
</Page>
